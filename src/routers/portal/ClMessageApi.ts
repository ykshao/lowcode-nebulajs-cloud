export = {
    'get /cl-message/new': async function (ctx, next) {
        // const { login } = ctx.state.user
        // const msgCount = await ClMessage.count({
        //     where: {
        //         login,
        //         appId: ctx.appId,
        //         read: false,
        //     },
        // })
        // ctx.ok({
        //     count: msgCount,
        // })
    },
}
